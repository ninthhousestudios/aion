{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if command -v sutra-guard >/dev/null 2>&1; then sutra-guard; fi"
          }
        ]
      }
    ]
  }
}
